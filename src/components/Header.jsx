import React from 'react';
import s from '../styles/Header.module.css'
const Header = () => {
    return (
        <div className={s.header}>
            <div className={s.title}>Title header </div>
        </div>
    );
};

export default Header;
